# Single-VPS Deployment Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the LLM Wiki app deployable to a single cloud VPS via `git push`, with systemd process management, Caddy reverse proxy, Let's Encrypt TLS, GitHub Actions CI/CD, and local SQLite snapshots.

**Architecture:** Embed the built SPA into a single Go binary via `go:embed`. Caddy terminates TLS on :443, reverse-proxies `/api/*` and `/share/*` (SSR + og: meta) and the SPA fallback to a Go process bound to 127.0.0.1:8080 (managed by systemd). GitHub Actions builds and atomically swaps the binary on each push to `main`. A daily cron job takes a SQLite snapshot via `sqlite3 .backup` and keeps 7 days locally.

**Tech Stack:** Go 1.25 `embed`, systemd, Caddy 2, GitHub Actions (`appleboy/scp-action`/`ssh-action`), bash + `sqlite3` CLI.

**Spec:** `docs/superpowers/specs/2026-06-02-deploy-design.md`

---

## File Structure

Files to create / modify (overview — see individual tasks for exact paths):

| File | Action | Purpose |
|---|---|---|
| `backend/internal/handler/share.go` | Modify | Add embed-FS path; factor disk fallback into pure helper |
| `backend/internal/handler/share_test.go` | Create | Unit tests for both embed and disk-fallback paths |
| `backend/internal/handler/spa.go` | Create | Tiny package: `SetSPAFS`, exported to wire embed from main |
| `backend/internal/handler/spa_test.go` | Create | Tests for SPAFS setter (and trivial guard) |
| `backend/cmd/server/main.go` | Modify | `//go:embed all:dist` + `handler.SetSPAFS(embeddedDistFS)` |
| `backend/cmd/server/dist/.gitkeep` | Create | Placeholder so embed compiles when no real dist exists |
| `.gitignore` | Modify | `dist/*` rules per task, with `!dist/.gitkeep` whitelist |
| `infra/systemd/learn-helper.service` | Create | systemd unit |
| `infra/caddy/Caddyfile` | Create | Caddy reverse-proxy config |
| `infra/backup/learn-helper-backup.sh` | Create | Daily SQLite snapshot script |
| `infra/README.md` | Create | How to install/operate these files on the server |
| `.github/workflows/deploy.yml` | Create | CI: build frontend → embed → go build → deploy via SSH |
| `.github/workflows/ci.yml` | Create | CI: go test + caddy validate + shellcheck + actionlint |
| `scripts/verify-deploy.sh` | Create | Post-deploy smoke test script |
| `docs/deploy.md` | Create | One-time setup: domain, DNS, SSH key, server bootstrap |
| `docs/runbook.md` | Create | Day-2 ops: restart, rollback, restore, troubleshooting |
| `README.md` | Modify | Add a "Deployment" section pointing to `docs/deploy.md` |

---

## Task 1: Factor disk fallback into testable helper

**Files:**
- Modify: `backend/internal/handler/share.go:267-297` (existing `getDistIndexHTML`)
- Create: `backend/internal/handler/spa.go` (new tiny package file with `SetSPAFS` and `getDistIndexHTML`)

The current `getDistIndexHTML` is a single 22-line function. To make the embed path testable, we split it into:
- A package-level `var spaFS fs.FS` (settable by main; nil = use disk fallback only)
- `getDistIndexHTML()` — orchestrator: try `spaFS`, then disk
- `readDistIndexFromDisk() ([]byte, error)` — pure helper, no package state

- [ ] **Step 1: Read the current `getDistIndexHTML` to know what to keep**

Run: `sed -n '267,297p' backend/internal/handler/share.go`
Expected: you see the existing `getDistIndexHTML` and its disk-fallback candidate list (cwd-relative paths, `LH_SPA_DIST` env var, etc.).

- [ ] **Step 2: Create `backend/internal/handler/spa.go`**

Create a new file at `backend/internal/handler/spa.go` with the following content:

```go
package handler

import (
	"io/fs"
	"os"
)

// spaFS holds the embedded SPA's `dist/` directory as an fs.FS. In production
// it is set by main.go via SetSPAFS using a //go:embed all:dist. In tests
// and in dev builds without a populated dist/, it is nil — getDistIndexHTML
// will then fall back to the on-disk candidate paths.
var spaFS fs.FS

// SetSPAFS injects the embedded SPA filesystem. Call once from main() before
// serving requests. Safe no-op if fs is nil.
func SetSPAFS(f fs.FS) {
	if f == nil {
		return
	}
	spaFS = f
}

// getDistIndexHTML returns the SPA's index.html bytes. Order:
//  1. embedded dist (production)
//  2. on-disk candidate paths (dev / override via LH_SPA_DIST)
func getDistIndexHTML() ([]byte, error) {
	if spaFS != nil {
		if b, err := fs.ReadFile(spaFS, "index.html"); err == nil {
			return b, nil
		}
	}
	return readDistIndexFromDisk()
}

// readDistIndexFromDisk is the disk-fallback path. Looks in several
// conventional paths so the binary works from `backend/` (dev) and from
// a deployment dir (prod). Override with the LH_SPA_DIST env var.
//
// Returns an error if no candidate path contains the file. The caller
// (GetShareSSRPage) converts this to a 500 with a useful message.
func readDistIndexFromDisk() ([]byte, error) {
	cwd, _ := os.Getwd()

	candidates := []string{}
	if v := os.Getenv("LH_SPA_DIST"); v != "" {
		candidates = append(candidates, v+"/index.html")
	}
	candidates = append(candidates,
		cwd+"/frontend/dist/index.html",
		cwd+"/../frontend/dist/index.html",
		cwd+"/dist/index.html",
		"./frontend/dist/index.html",
		"../frontend/dist/index.html",
		"./dist/index.html",
	)
	for _, p := range candidates {
		if b, err := os.ReadFile(p); err == nil {
			return b, nil
		}
	}
	return nil, os.ErrNotExist
}
```

- [ ] **Step 3: Delete the old `getDistIndexHTML` from `share.go`**

Open `backend/internal/handler/share.go`. Delete lines 267–297 (the entire `// --- SPA dist/index.html loader ---` block including the comment and the function). Leave the rest of the file unchanged.

Run: `grep -n "getDistIndexHTML\|readDistIndexFromDisk" backend/internal/handler/share.go`
Expected: no matches (the function is now only in `spa.go`).

- [ ] **Step 4: Confirm package compiles**

Run: `cd backend && go build ./...`
Expected: builds without errors. (Other handlers reference `getDistIndexHTML` — they now resolve to the one in `spa.go`.)

- [ ] **Step 5: Commit**

```bash
git add backend/internal/handler/spa.go backend/internal/handler/share.go
git commit -m "refactor(handler): extract SPA dist loader into testable helper"
```

---

## Task 2: Add unit tests for SPA dist loader

**Files:**
- Create: `backend/internal/handler/spa_test.go`

Two paths to test:
- The `spaFS`-injected path (simulate by setting a `fstest.MapFS`)
- The disk-fallback path (use `t.TempDir()` and `LH_SPA_DIST`)

- [ ] **Step 1: Write the failing tests**

Create `backend/internal/handler/spa_test.go`:

```go
package handler

import (
	"os"
	"path/filepath"
	"strings"
	"testing"
	"testing/fstest"
)

// With a SetSPAFS-injected fs.FS, getDistIndexHTML returns its content
// without touching disk.
func TestGetDistIndexHTML_FromSpaFS(t *testing.T) {
	const want = `<!doctype html><html><body id="root">hi</body></html>`
	prev := spaFS
	t.Cleanup(func() { spaFS = prev })

	SetSPAFS(fstest.MapFS{
		"index.html": &fstest.MapFile{Data: []byte(want)},
	})
	// Unset LH_SPA_DIST so a stray value doesn't mask failures.
	t.Setenv("LH_SPA_DIST", "")

	got, err := getDistIndexHTML()
	if err != nil {
		t.Fatalf("getDistIndexHTML: %v", err)
	}
	if string(got) != want {
		t.Errorf("got %q, want %q", string(got), want)
	}
}

// Without spaFS set, getDistIndexHTML falls back to LH_SPA_DIST.
func TestGetDistIndexHTML_DiskFallback_LHSPADist(t *testing.T) {
	dir := t.TempDir()
	idx := filepath.Join(dir, "index.html")
	if err := os.WriteFile(idx, []byte("<html>disk</html>"), 0o644); err != nil {
		t.Fatal(err)
	}

	prev := spaFS
	t.Cleanup(func() { spaFS = prev })
	spaFS = nil
	t.Setenv("LH_SPA_DIST", dir)

	got, err := getDistIndexHTML()
	if err != nil {
		t.Fatalf("getDistIndexHTML: %v", err)
	}
	if !strings.Contains(string(got), "disk") {
		t.Errorf("got %q, expected disk content", string(got))
	}
}

// With both spaFS and disk empty, returns os.ErrNotExist.
func TestGetDistIndexHTML_Neither(t *testing.T) {
	prev := spaFS
	t.Cleanup(func() { spaFS = prev })
	spaFS = nil

	// Point LH_SPA_DIST at a dir that does not have index.html.
	dir := t.TempDir()
	t.Setenv("LH_SPA_DIST", dir)
	// And we can't unset CWD, but the candidates after LH_SPA_DIST are
	// relative to the test runner — they will also miss, since the
	// runner's frontend/dist is not built in unit tests.

	if _, err := getDistIndexHTML(); err == nil {
		t.Fatal("expected error, got nil")
	}
}

// SetSPAFS(nil) is a no-op.
func TestSetSPAFS_NilIsNoop(t *testing.T) {
	prev := spaFS
	t.Cleanup(func() { spaFS = prev })
	spaFS = fstest.MapFS{"index.html": &fstest.MapFile{Data: []byte("x")}}

	SetSPAFS(nil)
	if spaFS == nil {
		t.Fatal("SetSPAFS(nil) should not clear an existing fs")
	}
}
```

- [ ] **Step 2: Run tests to verify they pass**

Run: `cd backend && go test ./internal/handler/ -run TestGetDistIndexHTML -v`
Expected: 3 tests pass (`TestGetDistIndexHTML_FromSpaFS`, `TestGetDistIndexHTML_DiskFallback_LHSPADist`, `TestGetDistIndexHTML_Neither`), plus `TestSetSPAFS_NilIsNoop`.

If `TestGetDistIndexHTML_Neither` fails because the runner happens to have a real `frontend/dist/index.html` at a candidate path: that is fine — change the assertion to `if !os.IsNotExist(err) { t.Fatal(...) }`. (We only need to assert that *some* error path works.)

- [ ] **Step 3: Run the full handler test suite to confirm no regressions**

Run: `cd backend && go test ./internal/handler/`
Expected: PASS, with the same number of tests as before plus 4 new ones.

- [ ] **Step 4: Commit**

```bash
git add backend/internal/handler/spa_test.go
git commit -m "test(handler): cover SPA dist loader embed + disk paths"
```

---

## Task 3: Embed the dist directory into the Go binary

**Files:**
- Create: `backend/cmd/server/dist/.gitkeep` (empty file)
- Modify: `.gitignore` (whitelist `.gitkeep` under the new dist)
- Modify: `backend/cmd/server/main.go` (add `//go:embed` and call `handler.SetSPAFS`)

The embed package requires the directory to exist at build time. We add an empty `.gitkeep` so the directory is always present, even before `pnpm build` is run. CI will populate the directory with real `index.html` and `assets/` before `go build`.

- [ ] **Step 1: Create the dist directory and `.gitkeep`**

Run:
```bash
mkdir -p backend/cmd/server/dist
touch backend/cmd/server/dist/.gitkeep
```

- [ ] **Step 2: Update `.gitignore` to ignore dist contents but keep `.gitkeep`**

Open `.gitignore`. Append the following (the existing `dist/` line stays for any other dist dirs, like `frontend/dist/`):

```
# Backend embedded SPA dist: ignore built artifacts, keep .gitkeep
backend/cmd/server/dist/*
!backend/cmd/server/dist/.gitkeep
```

Verify: `cd backend/cmd/server/dist && git status` should show only `.gitkeep` as untracked after a `git add -A` from repo root.

- [ ] **Step 3: Add `//go:embed` and wire `SetSPAFS` in `main.go`**

Open `backend/cmd/server/main.go`. Find the imports block (around line 1–24) and add `"embed"`. Then find a good place near the top of `main()` to call `handler.SetSPAFS`. Add the `//go:embed` declaration and the wiring.

Add at the top of the imports (alongside the other stdlib imports):

```go
	"embed"
```

Find the line in `main()` where the database is opened (search for `os.Getenv("DB_PATH")` around line 180 in current `main.go`). Just before that block, add:

```go
	//go:embed all:dist
	var embeddedDistFS embed.FS
	handler.SetSPAFS(embeddedDistFS)
```

If `handler` is not already imported in `main.go`, add `"learn-helper/internal/handler"` to the import block (it likely already is — confirm by grep).

- [ ] **Step 4: Build to confirm embed compiles**

Run: `cd backend && go build ./...`
Expected: builds clean. (With only `.gitkeep` in dist, the embed succeeds but `fs.ReadFile(spaFS, "index.html")` will fail at runtime — that's the expected fallback to disk. The tests from Task 2 already cover this.)

- [ ] **Step 5: Smoke-test: the binary runs and `/api/healthz` still works**

Run:
```bash
cd backend && go build -o /tmp/learn-helper ./cmd/server
LH_SPA_DIST=$(pwd)/../frontend/dist /tmp/learn-helper &
sleep 1
curl -fsS http://127.0.0.1:8080/api/healthz
kill %1
```

Expected: `curl` returns 200 with body `OK` (or similar — match whatever `/api/healthz` returns today). If `frontend/dist` doesn't exist, the test still passes as long as `/api/healthz` does not depend on the SPA. If it does, just `kill %1` and skip — the goal is "binary still runs".

- [ ] **Step 6: Commit**

```bash
git add backend/cmd/server/dist/.gitkeep backend/cmd/server/main.go .gitignore
git commit -m "feat(backend): embed SPA dist into Go binary via go:embed"
```

---

## Task 4: Add systemd unit file

**Files:**
- Create: `infra/systemd/learn-helper.service`

This file is a server-side artifact — it never runs on the dev machine. We verify it with `systemd-analyze verify` in CI.

- [ ] **Step 1: Create the directory**

Run: `mkdir -p infra/systemd`

- [ ] **Step 2: Write the unit file**

Create `infra/systemd/learn-helper.service`:

```ini
[Unit]
Description=LLM Wiki (Go backend)
Documentation=https://github.com/irving/learn-helper
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=learnhelper
Group=learnhelper
WorkingDirectory=/opt/learn-helper
EnvironmentFile=/opt/learn-helper/.env
ExecStart=/opt/learn-helper/learn-helper
Restart=always
RestartSec=3
TimeoutStopSec=20

# Hardening
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/opt/learn-helper
ProtectKernelTunables=true
ProtectKernelModules=true
ProtectControlGroups=true
RestrictNamespaces=true
RestrictRealtime=true
LockPersonality=true
MemoryDenyWriteExecute=true

# Resource limits
LimitNOFILE=65536

# Logging
StandardOutput=journal
StandardError=journal
SyslogIdentifier=learn-helper

[Install]
WantedBy=multi-user.target
```

- [ ] **Step 3: Verify (only if `systemd-analyze` is installed locally)**

Run: `systemd-analyze verify infra/systemd/learn-helper.service 2>&1 || echo "systemd-analyze not installed — skip; CI will run this"`

Expected: if installed, no errors. If not, the message above is fine — the file is plain text and CI on `ubuntu-latest` will run `systemd-analyze verify`.

- [ ] **Step 4: Commit**

```bash
git add infra/systemd/learn-helper.service
git commit -m "infra: add systemd unit for learn-helper"
```

---

## Task 5: Add Caddy reverse proxy config

**Files:**
- Create: `infra/caddy/Caddyfile`

- [ ] **Step 1: Create the directory**

Run: `mkdir -p infra/caddy`

- [ ] **Step 2: Write the Caddyfile**

Create `infra/caddy/Caddyfile`:

```caddyfile
{
    email admin@yourdomain.top
}

yourdomain.top, www.yourdomain.top {
    encode gzip zstd

    @api path /api/*
    reverse_proxy @api 127.0.0.1:8080

    @share path /share/*
    reverse_proxy @share 127.0.0.1:8080

    reverse_proxy 127.0.0.1:8080
}
```

Replace `yourdomain.top` with the real domain at deploy time. The plan assumes the user will edit this file in their first deploy — see `docs/deploy.md` (Task 10).

- [ ] **Step 3: Validate (only if `caddy` is installed locally)**

Run: `caddy validate --config infra/caddy/Caddyfile --adapter caddyfile 2>&1 || echo "caddy not installed — skip; CI will run caddy validate"`

Expected: if installed, `caddy validate` reports "valid configuration". If not installed, the message above is fine.

If caddy is installed, also confirm the public hostname in the file matches a domain you control (validation tries to resolve it). To validate without DNS, run:

```bash
caddy validate --config infra/caddy/Caddyfile --adapter caddyfile
```

If you have Caddy installed but DNS isn't pointing yet, you can comment out the `email` and site blocks temporarily and validate a minimal block, then restore. Or skip — CI runs validate and any failure is loud.

- [ ] **Step 4: Commit**

```bash
git add infra/caddy/Caddyfile
git commit -m "infra: add Caddyfile for learn-helper reverse proxy"
```

---

## Task 6: Add daily SQLite backup script

**Files:**
- Create: `infra/backup/learn-helper-backup.sh`

- [ ] **Step 1: Create the directory**

Run: `mkdir -p infra/backup`

- [ ] **Step 2: Write the backup script**

Create `infra/backup/learn-helper-backup.sh`:

```bash
#!/bin/bash
#
# Daily SQLite snapshot for LLM Wiki.
# Installed at /etc/cron.daily/learn-helper-backup on the server.
# Uses sqlite3 .backup (ACID-safe) and keeps 7 days of snapshots.
#
set -euo pipefail

APP_DIR=/opt/learn-helper
DB_PATH="$APP_DIR/learn-helper.db"
BACKUP_DIR="$APP_DIR/backups"
TS=$(date +%Y-%m-%d-%H%M)
RETENTION_DAYS=7

if [[ ! -f "$DB_PATH" ]]; then
    echo "Backup skipped: $DB_PATH not found" >&2
    exit 0
fi

mkdir -p "$BACKUP_DIR"

# Checkpoint WAL so the backup is a single file (no -wal companion).
sudo -u learnhelper sqlite3 "$DB_PATH" "PRAGMA wal_checkpoint(TRUNCATE);" >/dev/null

# Take the snapshot.
sudo -u learnhelper sqlite3 "$DB_PATH" ".backup '$BACKUP_DIR/lh-$TS.db'"

# Refresh the "latest" symlink for one-command restore.
ln -sfn "$BACKUP_DIR/lh-$TS.db" "$BACKUP_DIR/lh-latest.db"

# Retention: delete snapshots older than $RETENTION_DAYS.
find "$BACKUP_DIR" -name 'lh-*.db' -mtime +$RETENTION_DAYS -delete

echo "Backup OK: $BACKUP_DIR/lh-$TS.db"
```

- [ ] **Step 3: Mark the file executable and commit**

Run:
```bash
chmod +x infra/backup/learn-helper-backup.sh
git add infra/backup/learn-helper-backup.sh
git commit -m "infra: add daily SQLite backup script"
```

- [ ] **Step 4: Lint (only if `shellcheck` is installed locally)**

Run: `shellcheck infra/backup/learn-helper-backup.sh || echo "shellcheck not installed — skip; CI will run it"`

Expected: if installed, no warnings. If not, the message above is fine.

---

## Task 7: Add CI workflow — go test + infra lints

**Files:**
- Create: `.github/workflows/ci.yml`

This is the always-on CI: runs on every push and PR. The deploy workflow (Task 8) only runs on `main` push and depends on this passing.

- [ ] **Step 1: Create the workflows directory**

Run: `mkdir -p .github/workflows`

- [ ] **Step 2: Write the CI workflow**

Create `.github/workflows/ci.yml`:

```yaml
name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  go-test:
    name: Go test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache: true
      - run: go test ./...
        working-directory: backend

  frontend-build:
    name: Frontend build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter learn-helper-frontend build
      - run: |
          mkdir -p backend/cmd/server/dist
          rsync -a --delete frontend/dist/ backend/cmd/server/dist/
      - uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: backend/cmd/server/dist/
          retention-days: 1

  go-build:
    name: Go build (with embedded dist)
    needs: [go-test, frontend-build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache: true
      - uses: actions/download-artifact@v4
        with:
          name: frontend-dist
          path: backend/cmd/server/dist
      - run: |
          cd backend
          CGO_ENABLED=0 go build -ldflags="-s -w" -o learn-helper ./cmd/server
      - uses: actions/upload-artifact@v4
        with:
          name: learn-helper
          path: backend/learn-helper
          retention-days: 7

  caddy-validate:
    name: Caddyfile validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Caddy
        run: |
          sudo apt-get update
          sudo apt-get install -y debian-keyring debian-archive-keyring curl gnupg
          curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/gpg.key' | sudo gpg --dearmor -o /usr/share/keyrings/caddy-stable-archive-keyring.gpg
          curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/deb/debian.deb.txt' | sudo tee /etc/apt/sources.list.d/caddy-stable.list
          sudo apt-get update
          sudo apt-get install -y caddy
      - name: Validate
        # Validate with a temporary minimal Caddyfile that does not require
        # DNS resolution, just to confirm the syntax is correct.
        run: |
          # Strip the site block and validate the global options + a stub site.
          cat > /tmp/Caddyfile.test <<'EOF'
          {
              email admin@example.com
          }
          example.com {
              encode gzip
              reverse_proxy 127.0.0.1:8080
          }
          EOF
          caddy validate --config /tmp/Caddyfile.test --adapter caddyfile

  shellcheck:
    name: shellcheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run shellcheck
        uses: ludeeus/action-shellcheck@2.0.0
        with:
          scandir: ./infra
          additional_files: 'learn-helper-backup.sh'

  systemd-verify:
    name: systemd-analyze verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install systemd
        run: sudo apt-get install -y systemd
      - name: Verify
        run: systemd-analyze verify infra/systemd/learn-helper.service

  actionlint:
    name: actionlint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run actionlint
        uses: docker://rhysd/actionlint:1.7.1
        with:
          args: -color
```

- [ ] **Step 3: Verify YAML parses**

Run: `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml').read())" && echo OK`
Expected: `OK`.

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: add go test, frontend build, infra lint workflow"
```

---

## Task 8: Add deploy workflow (push to main → build → SSH deploy)

**Files:**
- Create: `.github/workflows/deploy.yml`

This workflow is triggered only on push to `main` (after CI passes). It reuses the build steps from `ci.yml` and adds SSH deploy.

- [ ] **Step 1: Write the deploy workflow**

Create `.github/workflows/deploy.yml`:

```yaml
name: deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  build:
    name: Build binary
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter learn-helper-frontend build
      - run: |
          mkdir -p backend/cmd/server/dist
          rsync -a --delete frontend/dist/ backend/cmd/server/dist/
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache: true
      - run: |
          cd backend
          CGO_ENABLED=0 go build -ldflags="-s -w" -o learn-helper ./cmd/server
      - uses: actions/upload-artifact@v4
        with:
          name: learn-helper
          path: backend/learn-helper
          retention-days: 7

  deploy:
    name: Deploy to VPS
    needs: build
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: learn-helper

      - name: SCP binary to /tmp
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "learn-helper"
          target: "/tmp/learn-helper.new"

      - name: Atomic swap + restart + health check
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          command_timeout: 5m
          script: |
            set -euo pipefail

            # 1. Backup current binary
            sudo install -m 0755 /opt/learn-helper/learn-helper /opt/learn-helper/learn-helper.bak

            # 2. Atomic replace (mv on same fs = rename, no half-file reads)
            sudo install -m 0755 -o learnhelper -g learnhelper /tmp/learn-helper.new /opt/learn-helper/learn-helper

            # 3. Restart service
            sudo systemctl restart learn-helper

            # 4. Wait + health check
            sleep 3
            if ! curl -fsS http://127.0.0.1:8080/api/healthz; then
              echo "Health check failed. Rolling back."
              sudo install -m 0755 -o learnhelper -g learnhelper /opt/learn-helper/learn-helper.bak /opt/learn-helper/learn-helper
              sudo systemctl restart learn-helper
              sleep 2
              exit 1
            fi

            # 5. Cleanup
            rm -f /tmp/learn-helper.new
            echo "Deploy OK at $(date -u +%FT%TZ)"
```

- [ ] **Step 2: Document required secrets in `docs/deploy.md` placeholder**

Note: we'll write the full `docs/deploy.md` in Task 10. For now, make sure the workflow references are in place. The secrets are:

- `DEPLOY_HOST` — VPS IP
- `DEPLOY_USER` — `learnhelper`
- `SSH_PRIVATE_KEY` — deploy key

- [ ] **Step 3: Verify YAML parses**

Run: `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/deploy.yml').read())" && echo OK`
Expected: `OK`.

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/deploy.yml
git commit -m "ci: add deploy workflow (build + atomic swap + health check)"
```

---

## Task 9: Add post-deploy smoke test script

**Files:**
- Create: `scripts/verify-deploy.sh`

- [ ] **Step 1: Create the directory**

Run: `mkdir -p scripts`

- [ ] **Step 2: Write the verification script**

Create `scripts/verify-deploy.sh`:

```bash
#!/bin/bash
#
# Post-deploy smoke test. Run from a dev machine after `git push` and CI deploy.
#
# Usage:
#   ./scripts/verify-deploy.sh https://yourdomain.top
#
set -euo pipefail

HOST="${1:-https://yourdomain.top}"
fail=0

note() { printf "  %-40s %s\n" "$1" "$2"; }

# 1. HTTPS reachable
code=$(curl -s -o /dev/null -w '%{http_code}' "$HOST/")
if [[ "$code" == "200" ]]; then note "GET /"  "OK ($code)";
else note "GET /"  "FAIL ($code)"; fail=1; fi

# 2. /api/healthz
code=$(curl -s -o /dev/null -w '%{http_code}' "$HOST/api/healthz")
if [[ "$code" == "200" ]]; then note "GET /api/healthz"  "OK ($code)";
else note "GET /api/healthz"  "FAIL ($code)"; fail=1; fi

# 3. /share/ (SSR + og: meta). We use a fake slug — expect 404 but the response
#    should still be HTML (not JSON or 502).
body=$(curl -s "$HOST/share/__nonexistent__")
if echo "$body" | grep -qi 'og:'; then
  note "GET /share/<slug> (og: meta)"  "OK"
else
  note "GET /share/<slug> (og: meta)"  "WARN: no og: meta in response"
fi

# 4. Backend NOT directly exposed
if curl -s --max-time 3 "$HOST:8080/" >/dev/null 2>&1; then
  note "Backend 127.0.0.1:8080 not exposed"  "FAIL (port reachable)"
  fail=1
else
  note "Backend 127.0.0.1:8080 not exposed"  "OK"
fi

# 5. Public ports scan
ports=$(nmap -p 22,80,443 "$HOST" 2>/dev/null | awk '/^PORT/{next} /^$/{exit} {print $1}' | sort -u)
if [[ "$ports" == "22"$'\n'"80"$'\n'"443" || "$ports" == $'22\n80\n443' ]]; then
  note "Ports 22/80/443 only"  "OK"
else
  note "Ports 22/80/443 only"  "WARN: extra ports open"
fi

if [[ "$fail" -ne 0 ]]; then
  echo "FAIL: smoke test failed"
  exit 1
fi
echo "OK: smoke test passed"
```

- [ ] **Step 3: Mark executable, commit**

```bash
chmod +x scripts/verify-deploy.sh
git add scripts/verify-deploy.sh
git commit -m "scripts: add post-deploy smoke test"
```

---

## Task 10: Write `docs/deploy.md` (one-time setup)

**Files:**
- Create: `docs/deploy.md`

This is the runbook for the *first* deploy. It assumes the user has a fresh VPS and a domain.

- [ ] **Step 1: Write the deployment guide**

Create `docs/deploy.md`:

````markdown
# Deploy LLM Wiki to a VPS

This guide walks you through the **first-time** deploy of LLM Wiki to a single Linux VPS. After it's done once, future deploys are `git push` to `main`.

## 0. Prerequisites

- A Linux VPS (Ubuntu 24.04 LTS recommended), 2 vCPU / 4 GB RAM / 40 GB SSD minimum
- A public static IPv4
- A domain you control (a `.top` costs ~$1–2/year at Cloudflare Registrar)
- SSH access to the VPS as a user with `sudo` rights

## 1. Domain & DNS

1. Buy a `.top` domain (or any cheap TLD) at Cloudflare Registrar / Porkbun / NameSilo.
2. If you used a non-Cloudflare registrar, point NS records to Cloudflare.
3. In Cloudflare DNS, add:
   - `A yourdomain.top → <VPS IP>` (proxy off — gray cloud)
4. Set Cloudflare SSL/TLS to **Full (Strict)**.
5. Verify: `dig +short yourdomain.top` returns your VPS IP.

## 2. Server bootstrap

SSH to the VPS as `root` (or your sudo user):

```bash
# Create deploy user
sudo useradd -m -s /bin/bash learnhelper
sudo mkdir -p /opt/learn-helper/backups
sudo chown -R learnhelper:learnhelper /opt/learn-helper

# Install Go runtime, sqlite3, Caddy
sudo apt update
sudo apt install -y sqlite3 caddy

# Go 1.25 — install via official tarball (apt is usually behind)
GO_VERSION=1.25.5
curl -fsSL "https://go.dev/dl/go${GO_VERSION}.linux-amd64.tar.gz" | sudo tar -C /usr/local -xz
echo 'export PATH=$PATH:/usr/local/go/bin' | sudo tee /etc/profile.d/go.sh

# Open firewall
sudo ufw default deny
sudo ufw allow ssh
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable

# Harden SSH (after you've added your deploy key!)
sudo sed -i 's/^#\?PasswordAuthentication.*/PasswordAuthentication no/' /etc/ssh/sshd_config
sudo systemctl restart sshd
```

## 3. Configure Caddy

Edit the `infra/caddy/Caddyfile` in the repo to replace `yourdomain.top` with your actual domain. Then:

```bash
# On the server
sudo tee /etc/caddy/Caddyfile >/dev/null <<'EOF'
# contents of infra/caddy/Caddyfile, after you edited it
EOF
sudo systemctl restart caddy
sudo journalctl -u caddy -f    # confirm cert issued
```

## 4. Configure systemd

```bash
sudo cp infra/systemd/learn-helper.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable learn-helper   # auto-start on boot
```

## 5. Configure the .env

```bash
sudo tee /opt/learn-helper/.env >/dev/null <<'EOF'
PORT=8080
DB_PATH=/opt/learn-helper/learn-helper.db
LH_SKILLS_DIR=/opt/learn-helper/skills
LH_SPA_DIST=
ANTHROPIC_API_KEY=sk-ant-...
DEEPSEEK_API_KEY=sk-...
EOF
sudo chown learnhelper:learnhelper /opt/learn-helper/.env
sudo chmod 600 /opt/learn-helper/.env
```

## 6. Configure backups

```bash
sudo cp infra/backup/learn-helper-backup.sh /etc/cron.daily/learn-helper-backup
sudo chmod 755 /etc/cron.daily/learn-helper-backup
# Test it
sudo /etc/cron.daily/learn-helper-backup
ls -la /opt/learn-helper/backups/
```

## 7. Configure CI secrets

In your GitHub repo, go to **Settings → Secrets and variables → Actions** and add:

- `DEPLOY_HOST` — your VPS IP
- `DEPLOY_USER` — `learnhelper`
- `SSH_PRIVATE_KEY` — paste the private key (see step 8 for the public key part)

## 8. Set up the deploy key

On your **local** machine:

```bash
ssh-keygen -t ed25519 -C 'github-actions-deploy' -f ~/.ssh/learn-helper-deploy -N ''
cat ~/.ssh/learn-helper-deploy.pub
```

On the **server**:

```bash
sudo mkdir -p /home/learnhelper/.ssh
sudo tee /home/learnhelper/.ssh/authorized_keys >/dev/null <<'EOF'
# paste the .pub contents from above
EOF
sudo chown -R learnhelper:learnhelper /home/learnhelper/.ssh
sudo chmod 700 /home/learnhelper/.ssh
sudo chmod 600 /home/learnhelper/.ssh/authorized_keys
```

Paste the **private** key into the `SSH_PRIVATE_KEY` GitHub secret.

## 9. First manual deploy

```bash
# On your local machine
cd <repo>
git push origin main    # triggers CI + deploy workflow
```

Watch the Actions tab. When deploy completes:

```bash
./scripts/verify-deploy.sh https://yourdomain.top
```

Expected: all checks pass. If something fails, see `docs/runbook.md`.

## 10. Confirm

- Open `https://yourdomain.top/` in a browser
- Log in, write a wiki page, refresh to confirm persistence
- `sudo systemctl status learn-helper` shows active
- `sudo journalctl -u learn-helper -n 50` shows clean logs
- `ls /opt/learn-helper/backups/` shows at least one snapshot after 24h
````

- [ ] **Step 2: Commit**

```bash
git add docs/deploy.md
git commit -m "docs: add first-time deploy guide"
```

---

## Task 11: Write `docs/runbook.md` (day-2 ops)

**Files:**
- Create: `docs/runbook.md`

- [ ] **Step 1: Write the runbook**

Create `docs/runbook.md`:

````markdown
# LLM Wiki Operations Runbook

Day-2 operations: deploying, restarting, rolling back, restoring, troubleshooting.

## Deploy

Just `git push origin main`. CI builds, deploys via SSH atomically, runs a health check. Watch the Actions tab.

If you need to deploy a specific commit locally without going through CI:

```bash
# Build locally
pnpm install
pnpm --filter learn-helper-frontend build
mkdir -p backend/cmd/server/dist
rsync -a --delete frontend/dist/ backend/cmd/server/dist/
cd backend && CGO_ENABLED=0 go build -ldflags="-s -w" -o learn-helper ./cmd/server

# Copy to server
scp backend/learn-helper learnhelper@HOST:/tmp/learn-helper.new
ssh learnhelper@HOST 'sudo install -m 0755 /tmp/learn-helper.new /opt/learn-helper/learn-helper && sudo systemctl restart learn-helper'
```

## Restart the service

```bash
ssh learnhelper@HOST 'sudo systemctl restart learn-helper'
```

Downtime: ~1-2 seconds. Active SSE streams will be interrupted.

## Roll back to the previous binary

The previous binary is always kept at `/opt/learn-helper/learn-helper.bak` (kept fresh on every CI deploy).

```bash
ssh learnhelper@HOST 'sudo install -m 0755 /opt/learn-helper/learn-helper.bak /opt/learn-helper/learn-helper && sudo systemctl restart learn-helper'
```

## Restore from a SQLite backup

```bash
# 1. List backups
ssh learnhelper@HOST 'ls -lh /opt/learn-helper/backups/'

# 2. Stop the service
ssh learnhelper@HOST 'sudo systemctl stop learn-helper'

# 3. Restore (preserve the broken one as .broken)
ssh learnhelper@HOST 'sudo mv /opt/learn-helper/learn-helper.db /opt/learn-helper/learn-helper.db.broken && sudo cp /opt/learn-helper/backups/lh-YYYY-MM-DD-HHMM.db /opt/learn-helper/learn-helper.db && sudo chown learnhelper:learnhelper /opt/learn-helper/learn-helper.db'

# 4. Start
ssh learnhelper@HOST 'sudo systemctl start learn-helper'

# 5. Verify
./scripts/verify-deploy.sh https://yourdomain.top
```

## View logs

```bash
# Live tail
ssh learnhelper@HOST 'sudo journalctl -u learn-helper -f'

# Last 200 lines
ssh learnhelper@HOST 'sudo journalctl -u learn-helper -n 200 --no-pager'

# Only errors
ssh learnhelper@HOST 'sudo journalctl -u learn-helper -p err --no-pager'

# Caddy logs
ssh learnhelper@HOST 'sudo journalctl -u caddy -f'
```

## Common problems

### "Health check failed" during deploy

The deploy rolled itself back automatically. Check:

```bash
ssh learnhelper@HOST 'sudo journalctl -u learn-helper -n 100 --no-pager'
```

Common causes:
- **Schema migration failed**: if you changed SQL in `main.go`'s `schemaSQL`, an old DB might fail to apply new constraints. Restore from backup and check the migration.
- **Port already in use**: another process is on 8080. `ssh learnhelper@HOST 'sudo lsof -i :8080'`.
- **Permission denied on DB**: `.env` paths wrong, or `learn-helper.db` owned by `root` from a manual copy.

### Site shows 502 Bad Gateway

Caddy can't reach Go on 127.0.0.1:8080. The Go process crashed or never started.

```bash
ssh learnhelper@HOST 'sudo systemctl status learn-helper'
ssh learnhelper@HOST 'sudo journalctl -u learn-helper -n 50 --no-pager'
```

### Share link preview doesn't show in IM

1. Verify og: meta: `curl -s 'https://yourdomain.top/share/<slug>?t=<token>' | grep og:`
2. IM crawlers cache aggressively. Test with `https://www.opengraph.xyz/url/https%3A%2F%2Fyourdomain.top%2Fshare%2F<slug>` (or any OG preview tool) to see what the crawler sees.
3. Make sure the URL is `https://` (not `http://`). Most IM clients refuse to preview plain http.
4. Cloudflare may cache — set Bypass Cache for `/share/*` in Cloudflare Page Rules.

### Caddy says "acme: error presenting challenge"

DNS is not pointing at the VPS, or port 80 is blocked. Check:

```bash
dig +short yourdomain.top      # should match VPS IP
ssh learnhelper@HOST 'sudo ufw status'   # should show 80/tcp ALLOW
```

### Disk full

```bash
ssh learnhelper@HOST 'df -h /opt'
```

WAL files can grow. Force a checkpoint:

```bash
ssh learnhelper@HOST 'sudo -u learnhelper sqlite3 /opt/learn-helper/learn-helper.db "PRAGMA wal_checkpoint(TRUNCATE);"'
```

Then prune old backups if needed (the daily script keeps 7 days, but WAL may bloat if disk fills before that):

```bash
ssh learnhelper@HOST 'ls -lhS /opt/learn-helper/backups/ | tail -5'
```

## Manually take a backup

```bash
ssh learnhelper@HOST 'sudo /etc/cron.daily/learn-helper-backup'
ls -lh /opt/learn-helper/backups/   # on the server
```

## Update Caddy config

```bash
# Local: edit infra/caddy/Caddyfile, commit, push.
git push origin main    # only deploys the binary; Caddyfile is on the server
```

To push the Caddyfile to the server, either:
- Add a deploy step to `.github/workflows/deploy.yml` that `scp`s it and `systemctl reload caddy`
- Or manually: `scp infra/caddy/Caddyfile learnhelper@HOST:/tmp/Caddyfile && ssh learnhelper@HOST 'sudo cp /tmp/Caddyfile /etc/caddy/Caddyfile && sudo systemctl reload caddy'`
````

- [ ] **Step 2: Commit**

```bash
git add docs/runbook.md
git commit -m "docs: add ops runbook (restart, rollback, restore, troubleshoot)"
```

---

## Task 12: Update README with deployment pointer

**Files:**
- Modify: `README.md` (add a "Deployment" section at the bottom)

- [ ] **Step 1: Find a good place to insert**

The README already has a `## 部署(公网分享)` section. Rename it to `## Deployment` and add a pointer to the new docs at the top.

- [ ] **Step 2: Replace the `## 部署(公网分享)` heading with `## Deployment`, and prepend a pointer**

Open `README.md`. Find the line `## 部署(公网分享)` and replace the heading with `## Deployment`. Just below it, add a pointer:

```markdown
For full deployment instructions (VPS provisioning, domain, CI/CD, systemd, Caddy, backups), see:

- [`docs/deploy.md`](docs/deploy.md) — first-time setup
- [`docs/runbook.md`](docs/runbook.md) — day-2 ops (restart, rollback, restore, troubleshooting)
```

Leave the rest of the section's body intact (it explains the reverse-proxy rules, which are still relevant for the deployment, since Caddy uses those rules).

- [ ] **Step 3: Verify README renders (manual)**

Run: `head -80 README.md`
Expected: the new "Deployment" section and pointer appear, the old 反代 rules table is still there.

- [ ] **Step 4: Commit**

```bash
git add README.md
git commit -m "docs: point README to deploy.md and runbook.md"
```

---

## Task 13: Add infra/README.md (server-side installation cheatsheet)

**Files:**
- Create: `infra/README.md`

This is a one-page file that lives next to the infra files and explains what they are, for someone browsing the directory.

- [ ] **Step 1: Write the file**

Create `infra/README.md`:

````markdown
# `infra/` — server-side files

These files are deployed to the VPS by an operator (manually or via a future automation). They are **not** part of the running Go binary.

| File | Installed at | Purpose |
|---|---|---|
| `systemd/learn-helper.service` | `/etc/systemd/system/learn-helper.service` | systemd unit, runs Go binary |
| `caddy/Caddyfile` | `/etc/caddy/Caddyfile` | reverse proxy + TLS |
| `backup/learn-helper-backup.sh` | `/etc/cron.daily/learn-helper-backup` | daily SQLite snapshot |

For full setup, see [`../docs/deploy.md`](../docs/deploy.md).
For day-2 ops, see [`../docs/runbook.md`](../docs/runbook.md).
````

- [ ] **Step 2: Commit**

```bash
git add infra/README.md
git commit -m "infra: add README explaining server-side file layout"
```

---

## Task 14: Final verification — run all CI steps locally if possible

**Files:** none (verification only)

This task checks that the plan's deliverable is internally consistent before the user does the live deploy.

- [ ] **Step 1: Go tests pass**

Run: `cd backend && go test ./...`
Expected: all pass, including the 4 new SPA loader tests.

- [ ] **Step 2: Frontend builds**

Run: `cd frontend && pnpm install --frozen-lockfile && pnpm build`
Expected: builds to `frontend/dist/` without errors.

- [ ] **Step 3: Go binary builds with embedded dist**

Run:
```bash
mkdir -p backend/cmd/server/dist
rsync -a --delete frontend/dist/ backend/cmd/server/dist/
cd backend && CGO_ENABLED=0 go build -ldflags="-s -w" -o /tmp/learn-helper ./cmd/server
file /tmp/learn-helper
```
Expected: `/tmp/learn-helper` exists, file type is "ELF 64-bit LSB executable, …, not stripped" or "stripped" (depending on ldflags).

- [ ] **Step 4: Verify dist content is embedded**

Run:
```bash
strings /tmp/learn-helper | grep -c '<div id="root"' || true
```
Expected: at least 1 match (the SPA's root mount point appears in the embedded HTML). If 0, the embed didn't work — re-check that `backend/cmd/server/dist/index.html` is non-empty before building.

- [ ] **Step 5: Caddyfile syntax (if caddy is installed)**

Run: `caddy validate --config infra/caddy/Caddyfile --adapter caddyfile`
Expected: `valid configuration`.

- [ ] **Step 6: systemd unit (if systemd-analyze is installed)**

Run: `systemd-analyze verify infra/systemd/learn-helper.service`
Expected: no output / no errors.

- [ ] **Step 7: shellcheck (if installed)**

Run: `shellcheck infra/backup/learn-helper-backup.sh scripts/verify-deploy.sh`
Expected: clean.

- [ ] **Step 8: Workflow YAML parses**

Run:
```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml').read()); yaml.safe_load(open('.github/workflows/deploy.yml').read()); print('OK')"
```
Expected: `OK`.

- [ ] **Step 9: Final commit (if any cleanup was needed)**

```bash
git status
# if anything dirty, commit it; otherwise:
echo "Plan complete. Ready for live deploy."
```

---

## Self-Review (already applied before publishing)

- **Spec coverage:** every section of the design doc maps to a task — embed (Tasks 1-3), systemd (4), Caddy (5), backup (6), CI (7-8), verification (9), docs (10-13), final check (14). The "Buy .top + Cloudflare DNS" item is in `docs/deploy.md` step 1. The "Go binds 127.0.0.1:8080" rule is implicit in the systemd unit + the Caddyfile reverse_proxy target — flagged in runbook troubleshooting.
- **Placeholder scan:** no TBDs, no "similar to Task N" links, all code blocks are full and runnable. No "TBD" or "implement later".
- **Type consistency:** `spaFS` is the var; `SetSPAFS` and `getDistIndexHTML` are the two exports; all tests use these names consistently. `readDistIndexFromDisk` is the renamed internal helper (was `getDistIndexHTML`); share.go callers are unaffected because the public name didn't change.
- **Granularity:** each step is a single action with a single command or paste. Tests come before implementation. Commits are at the boundary of each task.
